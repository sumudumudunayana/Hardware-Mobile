import React from "react";
import { View, Text, TouchableOpacity } from "react-native";

export default function ProductEditScreen() {
  return (
    <View style={{ flex: 1, padding: 16 }}>
      <Text style={{ fontSize: 22 }}>Add / Edit Product</Text>
    </View>
  );
}